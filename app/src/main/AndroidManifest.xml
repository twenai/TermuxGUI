<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Overlay floating menu -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Termux run command integration -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <!-- Support permissions commonly needed for command wrappers / background behavior -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <package android:name="com.termux" />
        <package android:name="com.termux.api" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TermuxGUI">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TermuxGUI.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <service
            android:name=".FloatingMenuService"
            android:exported="false" />
    </application>

</manifest>
